/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io'],
      }
};

// export default nextConfig;
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ['cdn.sanity.io'],
//   },
export default nextConfig;
//     runtime: 'nodejs', // Ensure it runs in Node.js instead of Edge
//   },
// };

// export default nextConfig;
